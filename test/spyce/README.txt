This folder contains unit tests for Spyce.

To run them, do the following:
- Install PyTest: `pip install pytest`
- Compile Spyce (see the main project readme for how to do that), and copy spyce.so into this folder
- Follow the instructions in README.txt in the "static" subfolder
- cd into this directory
- `pytest`
